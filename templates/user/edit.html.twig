{% extends 'base.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}
    <h1 class="mx-auto my-3">Modifier un utilisateur</h1>
    <div class="row">
        <div class="card col-7 col-lg-4 m-auto bg-primary text-primary-inverse p-0">
            <div class="card-body ">
                <h6>{{ user.email }}</h6>
                {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}

            </div>
        </div>
    </div>
    <hr>
    <div class="d-flex flex-column align-items-center">
        <a class="btn btn-info my-3" href="{{ path('app_user_index') }}">Retour à l'index</a>

        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
