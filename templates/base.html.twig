<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

		<link rel="icon" type="image/png" href="{{ asset('build/images/logo.PNG') }}"/>

		<title>
			{% block title %}EMMAUS CONNECT
			{% endblock %}
		</title>
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
		{% block nav %}
			{% include 'include/_navbar.html.twig' %}
		{% endblock %}
	<body>

		<main>
			{% block body %}{% endblock %}
		</main>

		<footer class="fixed-bottom">
			<hr class="footer-line bg-secondary border-3">
			<div class="d-flex justify-content-between">
				<img src="{{ asset('build/images/logo.png') }}" alt="" class="align-self-end">
				<img src="{{ asset('build/images/slogan.PNG') }}" alt="" class="align-self-end">
			</div>
		</footer>

	</body>
</html>
